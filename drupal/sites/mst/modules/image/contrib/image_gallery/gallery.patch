--- image_gallery.module	2009-05-22 16:32:29.000000000 -0300
+++ image_gallery.module	2009-07-17 16:46:02.000000000 -0300
@@ -390,11 +390,19 @@
 
   $content = '';
   if (count($galleries)) {
-    $content .= '<ul class="galleries">';
+    
+      // hack para dar sort pelo tid
+    $ordered_galleries = array();
     foreach ($galleries as $gallery) {
+	$ordered_galleries[$gallery->tid] = $gallery;
+    }
+    krsort($ordered_galleries);
+    
+    $content .= '<ul class="galleries">';
+    foreach ($ordered_galleries as $gallery) {
       $content .= '<li class="clear-block">';
       if ($gallery->count) {
-        $content .= l(image_display($gallery->latest, IMAGE_THUMBNAIL), 'image/tid/'. $gallery->tid, array('html' => TRUE));
+         $content .= l(image_display($gallery->latest, IMAGE_THUMBNAIL), 'image/tid/'. $gallery->tid, array('html' => TRUE));
       }
       $content .= "<h3>". l($gallery->name, 'image/tid/'. $gallery->tid) ."</h3>\n";
       $content .= '<div class="description">'. check_markup($gallery->description) ."</div>\n";
